<form novalidate>

    <div>
      <label>
        First Name:
        <input type="text" [(ngModel)]="person.firstName"
          name="personFirstName" myRequired #personFirstName="ngModel">
      </label>
      <span *ngIf="!personFirstName?.errors?.myRequired && personFirstName.touched">
        First name is a required field
      </span>

    </div>

  </form>
